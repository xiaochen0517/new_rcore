[package]
name = "os"
version = "0.1.0"
edition = "2024"

[dependencies]
log = "0.4"
user_lib = { path = "../user" }

[profile.release]
debug = true
opt-level = 0
strip = false
split-debuginfo = "packed"
