[package]
name = "user_lib"
version = "0.1.0"
edition = "2024"

[dependencies]
riscv = { git = "https://gitee.com/rcore-os/riscv", features = ["inline-asm"] }

[profile.release]
debug = true
opt-level = 0
strip = false
split-debuginfo = "packed"

[features]
test = []

[lib]
test = false
